<svg xmlns="http://www.w3.org/2000/svg" width="600" height="400">
  <style>
    .full-bleed { width: 100%; height: 100%; margin: 0; }
    .no-border { border: none; }
  </style>
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml" class="full-bleed">
      <iframe id="viewer" class="full-bleed no-border"></iframe>
      <script>
        <![CDATA[
        async function loadContent() {
          try {
            const response = await fetch('https://raw.githubusercontent.com/zaka13alt/sitesinglefile/refs/heads/main/idk.html');
            if (!response.ok) throw new Error('Network response failed');
            const rawHtml = await response.text();

            const iframe = document.getElementById('viewer');
            const iframeDoc = iframe.contentDocument || iframe.contentWindow.document;

            iframeDoc.open();
            iframeDoc.write(rawHtml);
            iframeDoc.close();
          } catch (err) {
            console.error('Error fetching or writing HTML:', err);
          }
        }

        loadContent();
        ]]>
      </script>
    </div>
  </foreignObject>
</svg>
